/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['winterbear.in',"storage.googleapis.com"], // Add any other domains you need to load images from
    },
  };
  
  module.exports = nextConfig;
